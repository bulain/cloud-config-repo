spring:
  cloud:
    stream:
      bindings:
        input:
          destination: cloud.task
          group: queue
        output:
          destination: cloud.task
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
